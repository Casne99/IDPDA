\documentclass[aspectratio=169]{beamer}

\usetheme{Madrid}
\usecolortheme{default}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[italian]{babel}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,automata,calc}
\usepackage{listings}

\title{Input Driven Pushdown Automata ed Edit Distances}
\subtitle{Presentazione del lavoro di tesi}
\author{Luca Casnedi}
\institute{Universit\`a degli Studi di Milano\\Corso di Laurea in Informatica}
\date{Anno Accademico 2024--2025}

% --- DECORAZIONI GLOBALI ---
% Barra colorata sottile in alto
\setbeamertemplate{headline}{%
  \leavevmode
  \hbox{%
    \color{blue!60!black}\rule{\paperwidth}{1.5pt}%
  }%
}
% Linea sottile in basso
\setbeamertemplate{footline}{%
  \leavevmode
  \hbox{%
    \color{gray!60}\rule{\paperwidth}{0.5pt}%
  }%
}
% Margine slide leggermente aumentato per non "toccare" le barre
\addtobeamertemplate{frametitle}{}{\vspace{-0.5em}}

\begin{document}
\setbeamertemplate{navigation symbols}{}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Obiettivi della tesi}
\begin{itemize}
  \item Modello degli \emph{Input Driven Pushdown Automata} (IDPDA).
  \item Calcolo della distanza di edit stringa-stringa e stringa-linguaggio.
  \item Collegamenti tra automi input-driven e problema del calcolo delle distanze di edit.
  \item Legami tra distanze di edit e questioni aperte in teoria della complessità.
\end{itemize}
\end{frame}

\begin{frame}{Input Driven Pushdown Automata}
Un Input Driven Pushdown Automata deterministico e' una settupla
\[
A=(\Sigma,Q,\Gamma,q_0,\perp,[\delta_a]_{a\in\Sigma},F),\quad
\Sigma = \Sigma_{+1} \cup \Sigma_{-1} \cup \Sigma_0.
\]

\vfill
\begin{columns}[T,onlytextwidth]
  \column{0.5\textwidth}
  \begin{itemize}
    \item $\Sigma_{+1}$: parentesi sinistre
    \item $\Sigma_{-1}$: parentesi destre
    \item $\Sigma_0$: simboli neutri
  \end{itemize}

  \column{0.5\textwidth}
  \begin{itemize}
    \item $\delta_{<}:Q\to Q\times\Gamma$.
    \item $\delta_{>}:Q\times(\Gamma\cup\{\perp\})\to Q$.
    \item $\delta_c:Q\to Q$.
  \end{itemize}
\end{columns}
\end{frame}

% Slide con diagramma a sinistra, divisorio nero centrale (fino in fondo) e liste a destra

\begin{frame}{Esempio di DIDPDA: diagramma}
\begin{columns}[T,onlytextwidth]
  \column{0.60\textwidth}
  \centering
  \begin{tikzpicture}[>=stealth, shorten >=1pt, auto, node distance=4cm, semithick, font=\scriptsize, scale=1.15, transform shape]
      \tikzstyle{every state}=[fill=white, draw=black, text=black, minimum size=12mm]
      \node[state, accepting] (q0) {$q_{0}$};
      \node[state, accepting, right=4cm of q0] (q1) {$q_{1}$};
      \node[state, below=3cm of $(q0)!0.5!(q1)$] (qerr) {$q_{\text{err}}$};
      % freccia iniziale dall'alto
      \draw[->] ([yshift=1cm]q0.north) -- (q0.north);
      % archi di transizione
      \path[->]
          (q0) edge[loop below] node[below left] {$<$/push $X$} ()
          (q0) edge[bend left=15] node[above] {$a$} (q1)
          (q0) edge node[left] {$>$/err} (qerr)
          (q1) edge[loop above] node[above] {$a$} ()
          (q1) edge[bend left=15] node[below] {$>$/pop $X$} (q0)
          (q1) edge node[right] {$>$/err} (qerr);
  \end{tikzpicture}%
  \hspace{2.5em}% Spazio aumentato tra diagramma e linea
  \column{0.015\textwidth}
  \begin{tikzpicture}[baseline={(current bounding box.center)}]
    \draw[black, thick] (0,0) -- (0,\dimexpr\textheight-2.5em\relax);
  \end{tikzpicture}
  \column{0.37\textwidth}
  \textbf{Stringhe accettate:}
  \begin{itemize}
    \item $<a>$
    \item $<a><a>$
    \item $aaa$
    \item \ldots
  \end{itemize}
  \vspace{0.5em}
  \textbf{Stringhe rifiutate:}
  \begin{itemize}
    \item $<a>>$
    \item $a>$
    \item $<>$
    \item \ldots
  \end{itemize}
\end{columns}
\vspace{0.5em}
\end{frame}

\begin{frame}{Distanze di edit: definizioni}
    \begin{center}
        \begin{tikzpicture}[font=\Large\ttfamily]
            % Stringa GATTO
            \node at (0,0) {G};
            \node at (1,0) {A};
            \node[red] at (2,0) {T};
            \node at (3,0) {T};
            \node at (4,0) {O};

            % Stringa GALLO
            \node at (0,-1) {G};
            \node at (1,-1) {A};
            \node[red] at (2,-1) {L};
            \node at (3,-1) {L};
            \node at (4,-1) {O};

            % Frecce e costi
            \draw[->, gray] (0, -0.3) -- (0, -0.7);
            \draw[->, gray] (1, -0.3) -- (1, -0.7);
            \draw[->, red, thick] (2, -0.3) -- (2, -0.7);
            \draw[->, red, thick] (3, -0.3) -- (3, -0.7);
            \draw[->, gray] (4, -0.3) -- (4, -0.7);

            \node[font=\normalsize] at (0, -1.5) {0};
            \node[font=\normalsize] at (1, -1.5) {0};
            \node[font=\normalsize, red] at (2, -1.5) {1};
            \node[font=\normalsize, red] at (3, -1.5) {1};
            \node[font=\normalsize] at (4, -1.5) {0};
        \end{tikzpicture}
    \end{center}

    \vfill
    La \textbf{distanza di Hamming} tra due stringhe della stessa lunghezza è il numero di posizioni in cui i simboli corrispondenti sono diversi.
    \[
        d_H(\text{GATTO}, \text{GALLO}) = 0 + 0 + 1 + 1 + 0 = 2
    \]
\end{frame}

\begin{frame}{Distanza stringa-linguaggio e correction problem}
Per $L\subseteq\Sigma^*$ e $x\in\Sigma^*$:
\[
d(L,x)=\min_{w\in L} d(x,w).
\]

\vspace{0.6em}
\textbf{Correction problem:} trovare una correzione ottima
\[
y=\arg\min_{w\in L} d(x,w).
\]
\end{frame}

\begin{frame}{$k$-vicinato di un linguaggio $L$}
Definizione induttiva del vicinato di edit:
\[
E_0(w)=\{w\},\quad
E_{k+1}(w)=E_k(w)\cup\bigcup_{w'\in E_k(w)}\bigcup_{a\in\Sigma}(ins_a(w')\cup del_a(w')).
\]

Per un linguaggio:
\[
E_k(L)=\bigcup_{w\in L}E_k(w).
\]

\begin{itemize}
  \item Risultato: la famiglia dei linguaggi IDPDA e' chiusa rispetto al $k$-vicinato.
\end{itemize}

\vfill
\footnotesize
\begin{thebibliography}{9}
  \bibitem{okhotin-salomaa-2018}
  A. Okhotin, K. Salomaa, 2018.
\end{thebibliography}
\end{frame}

\begin{frame}{Linguaggi con distanza di edit "facile"}
    \begin{itemize}
        \setlength{\itemsep}{1em}
        \item $O(n)$ per linguaggi regolari.
        \item $O(n^3)$ per linguaggi context-free.
        \begin{itemize}
            \item[$\rightarrow$] Caso particolare: linguaggi accettati da Input Driven Pushdown Automata.
        \end{itemize}
        \item La classe dei linguaggi riconosciuti da 1-NAuxPDA è la più ampia classe per cui si conosce un algoritmo efficiente per il calcolo della distanza di edit.
    \end{itemize}

    \vfill
    \footnotesize
    \begin{thebibliography}{9}
        \bibitem{pighizzini}
        G. Pighizzini, 2001.
    \end{thebibliography}
\end{frame}

\begin{frame}{Linguaggi con distanza di edit "difficile"}
\begin{columns}[T,onlytextwidth]
    \column{0.5\textwidth}
    \begin{itemize}
        \item P: problemi risolvibili in tempo polinomiale deterministico.
        \item NP: problemi con certificati verificabili in tempo polinomiale deterministico.
    \end{itemize}
    \column{0.5\textwidth}
    \vfill
    \centering
    Questione aperta: $\stackrel{?}{P=NP}$
    \vfill
\end{columns}

\begin{block}{Teorema}
Se $P=NP$, allora la distanza di edit di qualunque linguaggio in $NP$ e' calcolabile in tempo polinomiale.
\end{block}

Idea della prova:
\begin{itemize}
  \item Si considera il problema decisionale $\Pi_L$: ``$d(L,x)\le k$?''.
  \item Si indovina una stringa candidata $y$ di lunghezza polinomiale in $|x|$.
  \item Si verifica se $y \in L$ e $d(y, x) \le k$.
  \item Si calcola $d(L,x)$ valutando $\Pi_L$ per valori crescenti di $k$.
\end{itemize}
\end{frame}

\end{document}

